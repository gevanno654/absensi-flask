# absensi-flask

Import .sql di phpMyAdmin

# Install Program
jalankan perintah pertama:
pip install flask flask-cors opencv-contrib-python numpy pandas mysql-connector-python pillow

lalu jalankan perintah kedua:
pip install flask-sqlalchemy flask-mysql


# Run Program
jalankan perintah: python app.py
